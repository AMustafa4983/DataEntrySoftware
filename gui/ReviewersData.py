class ReviewersData():
    '''
        This class for adding data for reviewer,
        it makes the user add tests values for specific reveiwer.

        --> showWindow() - function to run the code that render the window
        --> setRecords() - assign reviewer tests score to variables or append it in a dict.
        --> getRecords() - return reviewer records.

        ---> windowTitle - string
        ---> reviewerName - string
    '''
